# Docs

Use this directory for supporting notes, research, and public-facing drafts (e.g., changelog, FAQ). Keep anything client-identifiable inside `/docs/private/` (add .gitignore entry later) or avoid committing sensitive data altogether.
