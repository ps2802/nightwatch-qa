name: Intake JSON to Issue (placeholder)

on:
  workflow_dispatch:
    inputs:
      payload:
        description: "Paste Typeform JSON"
        required: true
        type: string

jobs:
  create-issue:
    runs-on: ubuntu-latest
    steps:
      - name: Decode payload
        run: |
          echo "$PAYLOAD" > intake.json
        env:
          PAYLOAD: "${{ github.event.inputs.payload }}"
      - name: Print instructions
        run: |
          echo "TODO: parse intake.json and call gh issue create using client-intake template."
          echo "Store the raw JSON under workflow/intake-drops/ once automation is ready."
